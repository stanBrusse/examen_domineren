---
deployment:
  tasks:
    - export DEPLOYPATH=/home/bveens/public_html/sites/
    - /DTV -R pages $DEPLOYPATH
    - /DTV -R css $DEPLOYPATH
    - /DTV -R images $DEPLOYPATH
    - /DTV -R js $DEPLOYPATH
    - /DTV -R php $DEPLOYPATH